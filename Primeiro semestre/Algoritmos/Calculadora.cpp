#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

float divisao (int num1, int num2) {
	
}

float multiplicacao (int num1, int num2) {
	
}

int soma (int num1, int num2) {
	
}

int subtracao (int num1, int num2) {
	
}

int main () {
	int numero1, numero2, tipo;
	float acumulador;
	char op;
	
	printf ("Atenção o programa funciona da seguinte forma:\n");
	printf ("1 - Os números informados devem ser inteiros\n");
	printf ("2 - As contas deverão ser escritos separadamente\n");
	printf ("Exemplo: 12 + 12");
	system ("pause");
	system ("cls")
	while (op != 's') {
		printf ("Escreva a conta desejada (digite s para sair):  ");
		scanf ("%d %c %d", &numero1, &op, &numero2);
	}
	
			
	return 0;
}
